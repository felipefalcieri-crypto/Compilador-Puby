puts "Digite o valor de a:"
a = gets
puts "Digite o valor de b:"
b = gets
puts "Digite o valor de c:"
c = gets


a = a + 0
b = b + 0
c = c + 0


if a <= 0 or b <= 0 or c <= 0
    puts "Medidas inválidas"
elsif a + b <= c or a + c <= b or b + c <= a
    puts "Medidas inválidas"
else
    
    if a == b and b == c
        puts "Triângulo equilátero válido"
    elsif a == b or a == c or b == c
        puts "Triângulo isósceles válido"
    else
        puts "Triângulo escaleno válido"
    end
end
